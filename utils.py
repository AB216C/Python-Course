
#modules-Named utils to calculate maximum
def find_max(numbers):
    numbers = [4,5,8]
    maximum=numbers[0]

    for item in numbers:
        if item > maximum:
            maximum = item

return maximum()
